// import { execFile } from "child_process";
import path from "path";
// import cssProcess from '@parcel/css';
import config from "../config";
import { createReadStream } from "fs";

export default function assets(url: string, callback?: (code: string) => void) {
  // const filename: string = path.resolve(config.cssRoot, url[0] === '/' ? `.${url}` : url);
}