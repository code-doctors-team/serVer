{
  "name": "@sv-cd/server",
  "version": "0.0.1",
  "description": "Variant of express used in @sv-cd/core",
  "main": "build/index.js",
  "scripts": {
    "build": "esbuild ./src/index.js --minify --outfile=build/index.js --target=node12.0.1 --bundle --platform=node"
  },
  "keywords": [],
  "author": "alejo <adoa2705@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.11.12"
  },
  "dependencies": {
    "finalhandler": "^1.1.2",
    "path-to-regexp": "^6.2.0",
    "send": "^0.17.1"
  }
}
